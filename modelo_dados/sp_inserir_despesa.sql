CREATE PROCEDURE `sp_inserir_despesa` (
	in _dsDespesa				varchar(36),
    in _idTipoDespesa			varchar(36),
    in _idTipoEstabelecimento	varchar(36),
    in _dtDespesa	date,
    in _vlDespesa	double
)
BEGIN

END