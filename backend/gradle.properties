# Enable automatic download of Java toolchain matching the requested language version
org.gradle.java.installations.auto-download=true
# Recommended JVM args for CI/local builds
org.gradle.jvmargs=-Xmx1g
